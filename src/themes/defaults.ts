import Color from './Color';
import Fonts from './Fonts';

const { fontSizes, fontWeight } = Fonts;

export default {
  FONT_SIZE: fontSizes.medium,
  PRIMARY_COLOR: Color.cyan,
  SECONDARY_COLOR: Color.purple,
  FONT_WEIGHT: fontWeight.normal,

  FORM_MAX_WIDTH: 450,
};
